# Test Data (Phase 2)

Place simple, trainer-friendly test data files here.

Phase 3 will add realistic examples such as:

- `users.json` (demo users)
- `payments.json` (ParaBank-style payment inputs)
- `mobile-users.json` (mobile login personas)

