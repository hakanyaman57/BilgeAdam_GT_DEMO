# Trainer Demo Order

1. Repository tour
   - packages, feature folders, config, docs
2. Web technical lab first (The Internet)
   - dropdown, iframe, explicit wait, shadow DOM
3. Web business flow (ParaBank)
   - invalid login + registration form fill
4. Advanced DOM lab (SelectorsHub)
   - nested shadow + iframe edge case
5. API module (Restful-Booker, Karate + JUnit 5)
   - `GET /ping`, `POST /booking`, `GET /booking/{id}`, reusable token call
6. Mobile native (TheApp)
   - login and list validation fallback flow
7. Mobile hybrid (ApiDemos)
   - `NATIVE_APP` to `WEBVIEW` context switch
8. BDD classroom comparison
   - bad vs improved vs outline + Background + Rule
9. Anti-pattern lab (DO NOT USE)
   - wait, flaky, locator, data dependency, context, platform-locator failures
